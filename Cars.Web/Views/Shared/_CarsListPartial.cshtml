@model IEnumerable<Cars.Web.Models.CarViewModel>

<h2><u>Results:</u></h2>

@if (Model.Any())
{
   foreach (var car in Model)
   {
       Html.RenderPartial("_CarPartial", car);
   }
}
else
{
    <p>This owner has no cars</p>
}

